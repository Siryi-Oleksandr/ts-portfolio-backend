export interface IPosterFile extends Express.Multer.File {
  // Add any additional properties you might need here
  // For example, you can add a field to represent the file's original name:
  // originalname: string;
}
